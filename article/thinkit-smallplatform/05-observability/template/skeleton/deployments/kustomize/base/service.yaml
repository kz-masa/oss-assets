apiVersion: v1
kind: Service
metadata:
  name: ${{values.k8sResource.name}}
  labels:
    app: app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: http
    protocol: TCP
    name: http
  selector:
    app: app
